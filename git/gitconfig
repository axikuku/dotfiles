{{#if (and name email)}}
[user]
	name = {{name}}
	email = {{email}}
{{/if}}
[core]
	autocrlf = input
{{#if (is_executable "helix")}}
	editor = helix
{{/if}}
[init]
	defaultBranch = main
[pull]
	rebase = true
