{{#if (and name email)}}
[user]
	name = {{name}}
	email = {{email}}
{{/if}}
[color]
	ui = auto
[core]
	autocrlf = input
{{#if (is_executable "hx")}}
	editor = hx
{{/if}}
[init]
	defaultBranch = main
[pull]
	rebase = true